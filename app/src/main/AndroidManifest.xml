<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mymedicine">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DeleteMedicationActivity" />
        <activity android:name=".AssignPatientActivity" />
        <activity android:name=".DoctorFamilyHomepageActivity" />
        <activity android:name=".AssignMedicineActivity" />
        <activity
            android:name=".ElderlyHomepageActivity"
            android:label="@string/title_activity_elderly_homepage" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register" />
        <activity
            android:name=".RegisterElderlyActivity"
            android:label="Register as a patient" />
        <activity
            android:name=".ListMedicine"
            android:label="Medicines Database" />
        <activity
            android:name=".RegisterDoctorFamilyActivity"
            android:label="Register as carer" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>